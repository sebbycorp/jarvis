# PiCrawler Python dependencies
#
# SYSTEM-WIDE (installed on the Pi via SunFounder's official installers, NOT this file):
#   robot-hat 2.5.x   -> git clone -b 2.5.x https://github.com/sunfounder/robot-hat && sudo python3 install.py
#   vilib 0.3.18      -> git clone https://github.com/sunfounder/vilib && sudo python3 install.py
#   picrawler 2.1.4   -> sudo pip3 install ~/picrawler --break-system-packages
#   picamera2 0.3.36  -> apt python3-picamera2
#
# The app venv is created with --system-site-packages so it inherits all of the above.
# This file lists only OUR pip deps, installed INTO ~/picrawler-app/.venv:
fastmcp>=3.0
flask>=3.0
openai>=1.30
sounddevice>=0.4
opencv-python-headless>=4.8
